services:
  - type: cron
    name: keepalive-cron
    env: docker
    schedule: "* * * * *"   # runs every 1 minute
    command: |
      curl https://twilio-cascade-calling.onrender.com/health || echo "Health check failed"

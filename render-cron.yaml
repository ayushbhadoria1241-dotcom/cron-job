services:
  - type: cron
    name: keepalive-cron
    env: docker
    schedule: "* * * * *"   # runs every 1 minute
    command: |
      curl -s https://your-app.onrender.com/health || echo "Health check failed"
